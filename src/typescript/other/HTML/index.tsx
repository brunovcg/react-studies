import { Button } from './Button'
import { Input } from './Input'

export const Html = () => {
  return (
    <div>
      {' '}
      <Button variant="primary">Botão</Button>
      <Input />
    </div>
  )
}
